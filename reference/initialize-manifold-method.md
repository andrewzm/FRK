# manifold

Manifold initialisation. This function should not be called directly as
`manifold` is a virtual class.

## Usage

``` r
# S4 method for class 'manifold'
initialize(.Object)
```

## Arguments

- .Object:

  `manifold` object passed up from lower-level constructor
